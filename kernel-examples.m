x = -30:0.01:30;

logistic = 1./(1+exp(-x));
loggy = 2./(1+exp(-x)) - 1;
elu = (x >= 0).*x + (x < 0).*(exp(x)-1);
relie = (x >= 0).*x + (x < 0).*(x).*0.01;
ramp = (x>0).*x + 0.1*x;
leaky = (x>0).*x + (x<=0)*0.1.*x;
tanh = 2./(1+exp(-2.*x)) - 1;
plse = (x<-4).*(0.1*(x+4)) + (x>4).*((0.1*(x-4))+1) + (x>=-4).*(x<=4).*(0.125.*x+0.5);


plot(x, tanh, x, plse)